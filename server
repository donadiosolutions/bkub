#!/usr/bin/env python3
"""Launcher script to run the bootServer package via: python3 ./server

This avoids trying to execute the package directory directly.
"""
from __future__ import annotations

import runpy
import sys

if __name__ == "__main__":
    runpy.run_module("bootServer", run_name="__main__")
    sys.exit(0)
